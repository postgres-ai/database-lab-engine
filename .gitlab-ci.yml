variables:
  DOCKER_DRIVER: overlay2

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH && $CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH
      when: never

include:
  - template: Security/SAST.gitlab-ci.yml
    rules:
      - if: $SECURITY_AUDIT != "1"
  - local: 'engine/.gitlab-ci.yml'
    rules:
      - if: $SECURITY_AUDIT != "1"
  - local: 'ui/.gitlab-ci.yml'
    rules:
      - if: $SECURITY_AUDIT != "1"
  - local: '.gitlab-ci-security.yml'
    rules:
      - if: $SECURITY_AUDIT == "1"
  - project: 'postgres-ai/infra'
    file: '/ci/templates/approval-check.yml'
    rules:
      - if: $SECURITY_AUDIT != "1"
  - project: 'postgres-ai/infra'
    ref: '2f602b9f7df9e1100f095ef8e70823c216cb61a9'
    file: '/ci/templates/trivy-image-scan.yml'
    rules:
      - if: $SECURITY_AUDIT != "1"

empty-job:
  stage: test
  script:
    - echo 'success'
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: always
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - engine/**/*
        - ui/**/*
      when: never
