# Auto-cancel redundant pipelines when new commits are pushed
workflow:
  auto_cancel:
    on_new_commit: interruptible
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

default:
  image: golang:1.26.8
  interruptible: true  # All jobs can be cancelled by default

# Publishing has to come after the tests that qualify the artifact, and `needs:` cannot point at a
# later stage, so the two are ordered here rather than wired up job by job: `build-staging` produces
# the ref-scoped images the tests pull from the project registry, `e2e` runs them, and `publish` is
# the first stage allowed to push anything to Docker Hub or the release buckets.
stages:
  - test
  - build-binary
  - build-staging
  - e2e
  - publish
  - scan

variables:
  # The majors the build-image-*-pg-upgrade matrices publish a pg-upgrade image for. It has to stay
  # in step with those matrices: engine/test/6.clone_upgrade.sh treats a target on this list as one
  # it must be able to upgrade into, and fails instead of skipping when the images are missing.
  PG_UPGRADE_MAJORS: "17 18"

## Conditions.
.only_engine: &only_engine
  rules:
    - if: $CI_COMMIT_TAG =~ /^v[a-zA-Z0-9_.-]*/
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - engine/**/*
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      changes:
        - engine/**/*

.only_dle_tag_release: &only_tag_release
  interruptible: false  # Never cancel release jobs
  rules:
   - if: $CI_COMMIT_TAG =~ /^v[0-9.]+$/

.only_dle_tag_rc: &only_tag_rc
  interruptible: false  # Never cancel RC release jobs
  rules:
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+[\-_]*[a-zA-Z]+[a-zA-Z0-9.\-_]*[a-zA-Z0-9]+$/

# Both tag shapes at once, for the staging jobs that have to build the images the e2e matrix pulls
# no matter which kind of release is being cut.
.only_dle_tag_any: &only_tag_any
  interruptible: false
  rules:
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+$/
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+[\-_]*[a-zA-Z]+[a-zA-Z0-9.\-_]*[a-zA-Z0-9]+$/

.only_dle_master: &only_master
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      changes:
        - engine/**/*

.only_dle_feature: &only_feature
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - engine/**/*

# The tag-pipeline test set, referenced by every publish job so the gate is stated at each push site
# and not just implied by stage order. `artifacts: false` because publish jobs take their binaries
# from the build-binary stage; the bash jobs re-export engine/bin only as a debugging aid.
# These are the `.bash-test-main` matrix plus the Go integration suite - exactly the jobs whose rules
# below include the two v-tag patterns. `.bash-test-mr` jobs are absent on purpose: they never run on
# a tag, and naming them here would make every tag pipeline fail to generate.
.e2e_gate:
  needs:
    - job: bash-test-main-10
      artifacts: false
    - job: bash-test-main-11
      artifacts: false
    - job: bash-test-main-12
      artifacts: false
    - job: bash-test-main-13
      artifacts: false
    - job: bash-test-main-14
      artifacts: false
    - job: bash-test-main-15
      artifacts: false
    - job: bash-test-main-16
      artifacts: false
    - job: bash-test-main-17
      artifacts: false
    - job: bash-test-main-18
      artifacts: false
    - job: integration-test
      artifacts: false

# The image publish jobs additionally need the binaries the Dockerfiles COPY in. Naming `needs:`
# means artifacts arrive only from the jobs listed here, so build-binary-alpine has to be explicit -
# it is the job whose `make build` produces bin/dblab-server, bin/run-ci, bin/dblab and
# bin/rds-refresh.
.publish_image_gate:
  needs:
    - !reference [.e2e_gate, needs]
    - job: build-binary-alpine
      artifacts: true

test:
  <<: *only_engine
  stage: test
  script:
    - cd engine
    - make test

lint:
  <<: *only_engine
  stage: test
  script:
    - cd engine
    - make lint

### Build binary.
build-binary-alpine:
  <<: *only_engine
  image: golang:1.26.8-alpine
  stage: build-binary
  artifacts:
    paths:
      - engine/bin
  script:
    - apk add --update --no-cache make git
    - cd engine
    - make build

build-binary-client-dev:
  <<: *only_feature
  stage: build-binary
  artifacts:
    paths:
      - engine/bin
  script:
    - cd engine
    - make build-client

build-binary-client-branch:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_PROJECT_PATH == $CI_PROJECT_PATH'
      changes:
        - engine/**/*
  stage: build-binary
  artifacts:
    paths:
      - engine/bin
  script:
    - cd engine
    - make build-client

    # Install google-cloud-cli.
    - curl -fsSL https://packages.cloud.google.com/apt/doc/apt-key.gpg | gpg --dearmor -o /usr/share/keyrings/cloud.google.gpg
    - echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] http://packages.cloud.google.com/apt cloud-sdk main" | tee -a /etc/apt/sources.list.d/google-cloud-sdk.list
    - apt-get update && apt-get install -y google-cloud-cli

    # Authenticate.
    - GCP_KEY_FILE=$(mktemp) && trap "rm -f \"$GCP_KEY_FILE\"" EXIT && printf '%s' "${GCP_SERVICE_KEY}" > "$GCP_KEY_FILE" && gcloud auth activate-service-account --key-file="$GCP_KEY_FILE" && rm -f "$GCP_KEY_FILE"

    # Upload artifacts using MR IID to prevent cross-branch overwrites.
    - gsutil -m cp -r bin/cli/* gs://database-lab-cli/mr-${CI_MERGE_REQUEST_IID}/

build-binary-client-master:
  <<: *only_master
  stage: build-binary
  artifacts:
    paths:
      - engine/bin
  script:
    - cd engine
    - make build-client

    # Install google-cloud-cli.
    - curl -fsSL https://packages.cloud.google.com/apt/doc/apt-key.gpg | gpg --dearmor -o /usr/share/keyrings/cloud.google.gpg
    - echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] http://packages.cloud.google.com/apt cloud-sdk main" | tee -a /etc/apt/sources.list.d/google-cloud-sdk.list
    - apt-get update && apt-get install -y google-cloud-cli

    # Authenticate.
    - GCP_KEY_FILE=$(mktemp) && trap "rm -f \"$GCP_KEY_FILE\"" EXIT && printf '%s' "${GCP_SERVICE_KEY}" > "$GCP_KEY_FILE" && gcloud auth activate-service-account --key-file="$GCP_KEY_FILE" && rm -f "$GCP_KEY_FILE"

    # Upload artifacts.
    - gsutil -m cp -r bin/cli/* gs://database-lab-cli/master/

# The tag CLI binaries are built here and uploaded from the publish stage: the e2e jobs install
# bin/cli/dblab-linux-amd64 out of these artifacts, so the build cannot wait for the tests that
# consume it, while the upload to the release bucket must.
build-binary-client:
  <<: *only_tag_release
  stage: build-binary
  artifacts:
    paths:
      - engine/bin
  script:
    - cd engine
    - make build-client

build-binary-client-rc:
  <<: *only_tag_rc
  stage: build-binary
  artifacts:
    paths:
      - engine/bin
  script:
    - cd engine
    - make build-client

# Defaults to build-staging - the images the e2e matrix pulls from the project registry. The jobs
# that push to Docker Hub override this with `stage: publish` so they land after the tests.
.job_template: &build_image_definition
  image: docker:24
  stage: build-staging
  artifacts:
    paths:
      - engine/bin
  services:
    - name: docker:24-dind
      alias: docker
      command: [ "--tls=false" ]
  script:
    - cd engine
    - apk update && apk upgrade && apk add --no-cache bash # TODO(anatoly): Remove dependency.
    - bash ./scripts/ci_docker_build_push.sh

# The clone major upgrade image is built per target major from the matching extended-postgres
# image, so it is parameterized by PG_MAJOR through a parallel matrix. It deliberately does not
# reuse *build_image_definition: that template publishes engine/bin as artifacts, and this image
# contains no engine binary. The resulting tag mirrors the base tag (PG_UPGRADE_BASE_TAG), so
# pinning the base to "17-0.8.0" produces pg-upgrade:17-0.8.0.
.pg_upgrade_job_template: &build_pg_upgrade_image_definition
  image: docker:24
  stage: build-staging
  services:
    - name: docker:24-dind
      alias: docker
      command: [ "--tls=false" ]
  before_script:
    # Defaults to the moving major tag; set PG_UPGRADE_BASE_TAG on the job to pin a base such
    # as "17-0.8.0", which then also becomes the pg-upgrade tag.
    - export PG_UPGRADE_BASE_TAG="${PG_UPGRADE_BASE_TAG:-$PG_MAJOR}"
    - export BUILD_ARGS="--build-arg BASE_IMAGE=postgresai/extended-postgres:${PG_UPGRADE_BASE_TAG}"
    # The image carries scripts/pg_upgrade_clone.sh, so its contents are branch-specific and the
    # tag has to be too. Publishing every branch to the bare major would let concurrent MRs
    # overwrite each other in the shared project registry, and bash-test-17 would then pull and
    # exercise some other branch's upgrade script while still reporting its own result. Only the
    # release job leaves the suffix empty and publishes the bare major.
    - export TAGS="${DOCKER_NAME}:${PG_UPGRADE_BASE_TAG}${PG_UPGRADE_TAG_SUFFIX}"
  script:
    - cd engine
    - apk update && apk upgrade && apk add --no-cache bash
    - bash ./scripts/ci_docker_build_push.sh

build-image-feature-pg-upgrade:
  <<: *build_pg_upgrade_image_definition
  <<: *only_feature
  parallel:
    matrix:
      - PG_MAJOR: [ "17", "18" ]
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.pg-upgrade"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/pg-upgrade"
    PG_UPGRADE_TAG_SUFFIX: "-${CI_COMMIT_REF_SLUG}"

build-image-master-pg-upgrade:
  <<: *build_pg_upgrade_image_definition
  <<: *only_master
  parallel:
    matrix:
      - PG_MAJOR: [ "17", "18" ]
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.pg-upgrade"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/pg-upgrade"
    PG_UPGRADE_TAG_SUFFIX: "-${CI_COMMIT_REF_SLUG}"

# Tag pipelines run the same e2e matrix as master, and 6.clone_upgrade.sh / integration-test pull
# pg-upgrade:<major>-${CI_COMMIT_REF_SLUG} from the project registry. Without this job those tags do
# not exist on a tag and every upgrade test fails at `docker pull`.
build-image-tag-pg-upgrade:
  <<: *build_pg_upgrade_image_definition
  <<: *only_tag_any
  parallel:
    matrix:
      - PG_MAJOR: [ "17", "18" ]
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.pg-upgrade"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/pg-upgrade"
    PG_UPGRADE_TAG_SUFFIX: "-${CI_COMMIT_REF_SLUG}"

build-image-latest-pg-upgrade:
  <<: *build_pg_upgrade_image_definition
  <<: *only_tag_release
  stage: publish
  needs: !reference [.e2e_gate, needs]
  parallel:
    matrix:
      - PG_MAJOR: [ "17", "18" ]
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.pg-upgrade"
    DOCKER_NAME: "postgresai/pg-upgrade"
    # Released images are the ones the documented pgUpgradeImage default points at.
    PG_UPGRADE_TAG_SUFFIX: ""

build-image-feature-server:
  <<: *build_image_definition
  <<: *only_feature
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-server"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/dblab-server"
    TAGS: "${DOCKER_NAME}:${CI_COMMIT_REF_SLUG}"

build-image-feature-ci-checker:
  <<: *build_image_definition
  <<: *only_feature
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.ci-checker"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/dblab-ci-checker"
    TAGS: "${DOCKER_NAME}:${CI_COMMIT_REF_SLUG}"

build-image-feature-rds-refresh:
  <<: *build_image_definition
  <<: *only_feature
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.rds-refresh"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/dblab-rds-refresh"
    TAGS: "${DOCKER_NAME}:${CI_COMMIT_REF_SLUG}"

build-image-feature-client:
  <<: *build_image_definition
  <<: *only_feature
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-cli"
    DOCKER_NAME: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/dblab-cli"
    TAGS: "${DOCKER_NAME}:${CI_COMMIT_REF_SLUG}"

build-image-master-server:
  <<: *build_image_definition
  <<: *only_master
  variables:
    DOCKER_FILE: "Dockerfile.dblab-server"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-server"
    TAGS: "${DOCKER_NAME}:master,${DOCKER_NAME}:master-${CI_COMMIT_SHORT_SHA}"

build-image-master-ci-checker:
  <<: *build_image_definition
  <<: *only_master
  variables:
    DOCKER_FILE: "Dockerfile.ci-checker"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-ci-checker"
    TAGS: "${DOCKER_NAME}:master,${DOCKER_NAME}:master-${CI_COMMIT_SHORT_SHA}"

build-image-master-rds-refresh:
  <<: *build_image_definition
  <<: *only_master
  variables:
    DOCKER_FILE: "Dockerfile.rds-refresh"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-rds-refresh"
    TAGS: "${DOCKER_NAME}:master,${DOCKER_NAME}:master-${CI_COMMIT_SHORT_SHA}"

build-image-master-client:
  <<: *build_image_definition
  <<: *only_master
  variables:    
    DOCKER_FILE: "Dockerfile.dblab-cli"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-cli"
    TAGS: "${DOCKER_NAME}:master,${DOCKER_NAME}:master-${CI_COMMIT_SHORT_SHA}"

trivy-engine-images-feature:
  extends: .trivy_image_scan
  <<: *only_feature
  needs:
    - build-image-feature-server
    - build-image-feature-ci-checker
    - build-image-feature-rds-refresh
    - build-image-feature-client
  variables:
    TRIVY_IMAGES: >-
      ${CI_REGISTRY_IMAGE}/dblab-server:${CI_COMMIT_REF_SLUG}
      ${CI_REGISTRY_IMAGE}/dblab-ci-checker:${CI_COMMIT_REF_SLUG}
      ${CI_REGISTRY_IMAGE}/dblab-rds-refresh:${CI_COMMIT_REF_SLUG}
      ${CI_REGISTRY_IMAGE}/dblab-cli:${CI_COMMIT_REF_SLUG}

trivy-engine-images-master:
  extends: .trivy_image_scan
  <<: *only_master
  needs:
    - build-image-master-server
    - build-image-master-ci-checker
    - build-image-master-rds-refresh
    - build-image-master-client
  variables:
    TRIVY_IMAGES: >-
      ${CI_REGISTRY_IMAGE}/dblab-server:master-${CI_COMMIT_SHORT_SHA}
      ${CI_REGISTRY_IMAGE}/dblab-ci-checker:master-${CI_COMMIT_SHORT_SHA}
      ${CI_REGISTRY_IMAGE}/dblab-rds-refresh:master-${CI_COMMIT_SHORT_SHA}
      ${CI_REGISTRY_IMAGE}/dblab-cli:master-${CI_COMMIT_SHORT_SHA}

build-image-latest-server:
  <<: *build_image_definition
  <<: *only_tag_release
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-server"
    DOCKER_NAME: "postgresai/dblab-server"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export LATEST_TAG=$(echo ${CLEAN_TAG%.*}-latest)
    - export TAGS="${DOCKER_NAME}:${LATEST_TAG},${DOCKER_NAME}:${CLEAN_TAG}"

# The project-registry ("-dev") images are the release candidate under test, so they stay in
# build-staging rather than being gated on the tests that consume them. This one also carries the
# ref-scoped tag the bash matrix pulls as dblab-server:${CI_COMMIT_REF_SLUG}, which is why a tag
# pipeline needs no separate staging build of the server image.
build-image-latest-server-dev:
  <<: *build_image_definition
  <<: *only_tag_release
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-server"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-server"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG},${DOCKER_NAME}:${CI_COMMIT_REF_SLUG}"

build-image-latest-ci-checker:
  <<: *build_image_definition
  <<: *only_tag_release
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.ci-checker"
    DOCKER_NAME: "postgresai/dblab-ci-checker"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export LATEST_TAG=$(echo ${CLEAN_TAG%.*}-latest)
    - export TAGS="${DOCKER_NAME}:${LATEST_TAG},${DOCKER_NAME}:${CLEAN_TAG}"

build-image-latest-ci-checker-dev:
  <<: *build_image_definition
  <<: *only_tag_release
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.ci-checker"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-ci-checker"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"

build-image-latest-rds-refresh:
  <<: *build_image_definition
  <<: *only_tag_release
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.rds-refresh"
    DOCKER_NAME: "postgresai/dblab-rds-refresh"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export LATEST_TAG=$(echo ${CLEAN_TAG%.*}-latest)
    - export TAGS="${DOCKER_NAME}:${LATEST_TAG},${DOCKER_NAME}:${CLEAN_TAG}"

build-image-latest-rds-refresh-dev:
  <<: *build_image_definition
  <<: *only_tag_release
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.rds-refresh"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-rds-refresh"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"

build-image-latest-client:
  <<: *build_image_definition
  <<: *only_tag_release
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-cli"
    DOCKER_NAME: "postgresai/dblab"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export LATEST_TAG=$(echo ${CLEAN_TAG%.*}-latest)
    - export TAGS="${DOCKER_NAME}:${LATEST_TAG},${DOCKER_NAME}:${CLEAN_TAG}"

build-image-rc-server:
  <<: *build_image_definition
  <<: *only_tag_rc
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-server"
    DOCKER_NAME: "postgresai/dblab-server"

# Carries the ref-scoped tag the bash matrix pulls, same as the release variant above.
build-image-rc-server-dev:
  <<: *build_image_definition
  <<: *only_tag_rc
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG},${DOCKER_NAME}:${CI_COMMIT_REF_SLUG}"
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-server"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-server"

build-image-rc-ci-checker:
  <<: *build_image_definition
  <<: *only_tag_rc
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.ci-checker"
    DOCKER_NAME: "postgresai/dblab-ci-checker"

build-image-rc-ci-checker-dev:
  <<: *build_image_definition
  <<: *only_tag_rc
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.ci-checker"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-ci-checker"

build-image-rc-rds-refresh:
  <<: *build_image_definition
  <<: *only_tag_rc
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.rds-refresh"
    DOCKER_NAME: "postgresai/dblab-rds-refresh"

build-image-rc-rds-refresh-dev:
  <<: *build_image_definition
  <<: *only_tag_rc
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"
  variables:
    REGISTRY_USER: "${CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${CI_REGISTRY_PASSWORD}"
    REGISTRY: "${CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.rds-refresh"
    DOCKER_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-rds-refresh"

build-image-rc-client:
  <<: *build_image_definition
  <<: *only_tag_rc
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_NAME}:${CLEAN_TAG}"
  variables:
    REGISTRY_USER: "${DH_CI_REGISTRY_USER}"
    REGISTRY_PASSWORD: "${DH_CI_REGISTRY_PASSWORD}"
    REGISTRY: "${DH_CI_REGISTRY}"
    DOCKER_FILE: "Dockerfile.dblab-cli"
    DOCKER_NAME: "postgresai/dblab"

build-image-swagger-release:
  <<: *build_image_definition
  <<: *only_tag_release
  stage: publish
  needs: !reference [.publish_image_gate, needs]
  variables:
    DOCKER_FILE: "Dockerfile.swagger-ui"
    DOCKER_IMAGE_NAME: "registry.gitlab.com/postgres-ai/database-lab/dblab-swagger-ui"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})
    - export TAGS="${DOCKER_IMAGE_NAME}:${CLEAN_TAG}"

### Publish the CLI binaries built in build-binary, once the tests that exercised them have passed.
.gcs_cli_upload: &gcs_cli_upload
  stage: publish
  script:
    # Install google-cloud-cli.
    - curl -fsSL https://packages.cloud.google.com/apt/doc/apt-key.gpg | gpg --dearmor -o /usr/share/keyrings/cloud.google.gpg
    - echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] http://packages.cloud.google.com/apt cloud-sdk main" | tee -a /etc/apt/sources.list.d/google-cloud-sdk.list
    - apt-get update && apt-get install -y google-cloud-cli

    # Authenticate.
    - GCP_KEY_FILE=$(mktemp) && trap "rm -f \"$GCP_KEY_FILE\"" EXIT && printf '%s' "${GCP_SERVICE_KEY}" > "$GCP_KEY_FILE" && gcloud auth activate-service-account --key-file="$GCP_KEY_FILE" && rm -f "$GCP_KEY_FILE"

    # Upload artifacts.
    - cd engine
    - gsutil -m cp -r bin/cli/* "gs://database-lab-cli/${CLEAN_TAG}/"
    - if [ "${UPLOAD_LATEST}" = "true" ]; then gsutil -m cp -r bin/cli/* gs://database-lab-cli/latest/; fi

publish-binary-client:
  <<: *gcs_cli_upload
  <<: *only_tag_release
  needs:
    - !reference [.e2e_gate, needs]
    - job: build-binary-client
      artifacts: true
  variables:
    UPLOAD_LATEST: "true"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})

publish-binary-client-rc:
  <<: *gcs_cli_upload
  <<: *only_tag_rc
  needs:
    - !reference [.e2e_gate, needs]
    - job: build-binary-client-rc
      artifacts: true
  variables:
    # A release candidate never becomes the "latest" download.
    UPLOAD_LATEST: "false"
  before_script:
    - export CLEAN_TAG=$(echo ${CI_COMMIT_TAG#"v"})

# Base template for bash integration tests
.bash-test-base: &bash_test_base
  stage: e2e
  variables:
    IMAGE_TAG: "${CI_COMMIT_REF_SLUG}"
  artifacts:
    paths:
      - engine/bin
  before_script:
    - bash engine/test/_cleanup.sh
  script:
    - bash engine/test/1.synthetic.sh
    - bash engine/test/2.logical_generic.sh
    - bash engine/test/4.physical_basebackup.sh
    # Runs the upgrade for a POSTGRES_VERSION whose next major is in PG_UPGRADE_MAJORS and reports a
    # skip for the rest; either way it prints an UPGRADE_TEST_RESULT line naming the version pair.
    # The marker is asserted rather than merely printed when the job is one that must upgrade, so
    # coverage cannot quietly disappear behind a passing pipeline.
    - |
      # pipefail so that tee cannot mask a failing test run - the exact way a green pipeline
      # comes to claim coverage it never produced.
      set -o pipefail
      bash engine/test/6.clone_upgrade.sh 2>&1 | tee upgrade_test.log
    - |
      if [ "${UPGRADE_TEST_REQUIRED}" = "true" ] && ! grep -q "UPGRADE_TEST_RESULT: executed" upgrade_test.log; then
        echo "ERROR: this job must run a clone upgrade, but no UPGRADE_TEST_RESULT: executed was reported" >&2
        exit 1
      fi
  after_script:
    - bash engine/test/_cleanup.sh
  tags:
    - dle-test

# MR pipelines: Only run PG 17 and PG 18 to reduce queue time
# (10 jobs competing for 2 runners was causing 35+ min queue times)
.bash-test-mr: &bash_test_mr
  <<: *bash_test_base
  rules:
    - if: '$CI_PROJECT_NAMESPACE != "postgres-ai"'
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - engine/**/*

# Main branch and release tags: run the full test matrix for comprehensive coverage. The tag rules
# carry no `changes:` clause - a tag pipeline has no meaningful diff to compare against, and a
# release has to be qualified on the whole matrix regardless of what the last commit touched.
.bash-test-main: &bash_test_main
  <<: *bash_test_base
  rules:
    - if: '$CI_PROJECT_NAMESPACE != "postgres-ai"'
      when: never
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      changes:
        - engine/**/*
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+$/
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+[\-_]*[a-zA-Z]+[a-zA-Z0-9.\-_]*[a-zA-Z0-9]+$/

# MR jobs - only PG 17 and PG 18 (latest stable versions)
bash-test-17:
  <<: *bash_test_mr
  variables:
    POSTGRES_VERSION: 17
    # 17 -> 18 is the pair this pipeline publishes both images for, so this job has to run the
    # clone upgrade; 6.clone_upgrade.sh fails instead of skipping when it cannot.
    UPGRADE_TEST_REQUIRED: "true"

bash-test-18:
  <<: *bash_test_mr
  variables:
    POSTGRES_VERSION: 18

# Main branch jobs - full matrix (all PostgreSQL versions)
bash-test-main-10:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 10

bash-test-main-11:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 11

bash-test-main-12:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 12

bash-test-main-13:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 13

bash-test-main-14:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 14

bash-test-main-15:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 15

bash-test-main-16:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 16

bash-test-main-17:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 17
    UPGRADE_TEST_REQUIRED: "true"

bash-test-main-18:
  <<: *bash_test_main
  variables:
    POSTGRES_VERSION: 18

integration-test:
  services:
    - name: docker:24-dind
      alias: docker
      command: [ "--tls=false" ]
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - engine/**/*
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+$/
    - if: $CI_COMMIT_TAG =~ /^v[0-9.]+[\-_]*[a-zA-Z]+[a-zA-Z0-9.\-_]*[a-zA-Z0-9]+$/
  stage: e2e
  retry:
    max: 2
    when: script_failure
  variables:
    # Instruct Testcontainers to use the daemon of DinD.
    # DOCKER_HOST: "tcp://docker:2375"
    # Instruct Docker not to start over TLS.
    DOCKER_TLS_CERTDIR: ""
    # Improve performance with overlayfs.
    DOCKER_DRIVER: overlay2
    # Maximum supported API version is 1.41 for the current image
    DOCKER_API_VERSION: "1.41"
    # The clone upgrade tests run pg_upgrade in this image. The build stage precedes this one and
    # publishes it under the target major's ref-scoped tag; the tests authenticate with
    # CI_REGISTRY_USER and CI_REGISTRY_PASSWORD. This is the Go integration suite only - the bash
    # suite reads PG_UPGRADE_IMAGE, which it derives per job.
    #
    # These tests read a clone directory that containers write, so they need a daemon that shares
    # this job's filesystem. The docker:dind service below does not provide that, so the upgrade
    # tests currently SKIP here rather than run - `make test-ci-integration` passes -v so the trace
    # says so plainly. Moving this job to a runner whose daemon is local (the dle-test shell
    # runners are) and setting DBLAB_UPGRADE_TESTS_REQUIRED: "true" turns those skips into
    # failures and makes the coverage real; until then this job does not exercise pg_upgrade.
    DBLAB_PG_UPGRADE_IMAGE: "${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}/pg-upgrade:17-${CI_COMMIT_REF_SLUG}"
  script:
    - cd engine
    - make test-ci-integration
  tags:
    # The suite reaches the daemon over unix:///var/run/docker.sock, which only the hetzner docker
    # runners bind-mount - the dind service above is not reachable that way, and DOCKER_HOST stays
    # commented out above for the filesystem-sharing reason already noted. Instance runners expose
    # no such socket, so every test that starts a container fails there. Both tags are required so
    # this lands on hetzner-gitlab-runner-01/02 and not on the dle-test shell runners.
    - hetzner-runner
    - docker
